{
	"private": true,
	"name": "mikka-bouzo-client",
	"version": "0.0.0",
	"license": "MIT",
	"scripts": {
		"start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
		"build": "preact build",
		"serve": "preact build && preact serve",
		"dev": "preact watch"
	},
	"dependencies": {
		"preact": "^8.2.9",
		"preact-compat": "^3.18.0",
		"preact-router": "^2.6.0",
		"skeleton-css": "^2.0.4",
		"sockette": "^2.0.0",
		"stockroom": "^1.0.1",
		"unistore": "^3.0.5"
	},
	"devDependencies": {
		"if-env": "^1.0.0",
		"preact-cli": "^2.2.1",
		"preact-cli-plugin-fast-async": "^1.0.0",
		"workerize-loader": "^1.0.2"
	}
}
